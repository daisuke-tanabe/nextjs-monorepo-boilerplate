{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "dev": {
      "dependsOn": ["^migrate"],
      "cache": false,
      "persistent": true
    },
    "migrate": {},
    "test": {}
  }
}
