{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^generate", "^build"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "db:push": {
      "cache": false
    },
    "dev": {
      "dependsOn": ["^generate", "^db:push"],
      "cache": false,
      "persistent": true
    },
    "generate": {
      "cache": false
    },
    "test": {}
  }
}
